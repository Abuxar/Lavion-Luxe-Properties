# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/
out/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Vercel
.vercel/

# Next.js generated agent guidance (regenerated by `next dev`)
# kept in-tree deliberately; see apps/web/AGENTS.md

# ---------------------------------------------------------------
# Secrets & credentials — never commit any of these.
# .env.example is the ONLY env file in the repo and holds placeholders.
# ---------------------------------------------------------------
.env
.env.*
!.env.example
*.local

# Private keys, certificates, keystores
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.jks
*.keystore
id_rsa*
id_ecdsa*
id_ed25519*
*.ppk

# Cloud / service credentials
*credentials*.json
*service-account*.json
serviceAccountKey.json
gha-creds-*.json
.aws/
.gcloud/
secrets.json
secrets.yml
secrets.yaml

# Tool auth
.npmrc
.netrc
.pypirc

# Platform link/state dirs (contain project + org ids, OIDC tokens)
.vercel
.turbo/

# DB dumps
*.sql
*.dump
*.bson
